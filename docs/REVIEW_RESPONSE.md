# ردّ المراجعات الخارجية — ما نُفّذ وما أُجِّل
### (Gemini · ChatGPT · Base44) — جلسة التحصين

> هذا سجل لما عالجناه من ملاحظات المراجعين الثلاثة، وما أُجِّل ولماذا. كل بند منفّذ
> له commit على `main`. التواريخ تقريبية حسب ترتيب العمل.

---

## ✅ نُفِّذ (Done)

### الأمان والأداء (Migration 062 + كود)
| البند | ماذا عُمل |
|---|---|
| **إخفاء هوية المقاول على مستوى الخادم** (Critical في كل المراجعات) | `profiles_public` صار يُخفي اسم المقاول؛ دالة `get_rfq_contractor()` تكشف الاسم فقط لصاحب الطلب/الأدمن/مورد قُبل عرضه. صفحات المورد ما عادت تجلب الاسم قبل القبول. |
| **`is_counterparty()` داخل RLS = بطء** (Critical أداء) | استُبدلت باستعلام `EXISTS` مضمّن + فهارس على `offers(supplier_id)`, `offers(rfq_id)`, `rfqs(contractor_id)`. |
| **Bid Sniping** (قنص آخر دقيقة) | `get_rfq_offer_ranking()` يُخفي أسعار المنافسين ويعيد `blind=true` في آخر **30 دقيقة**؛ واجهة المورد تعرض «المراكز مخفية». |
| **عروض المنافسين** | تحقّقنا: سياسة `offers` تمنع المورد من رؤية عروض غيره أصلاً ✓ (لا تسريب). |
| **`handle_new_user` يبتلع الأخطاء** | تحقّقنا: المسار الحرج يسجّل في `system_errors_log` ✓. |
| **`market_price_snapshots` يكشف منافس** | تحقّقنا: `get_market_prices/trend` تُرجع تجميعات فقط بلا `supplier_id` ✓. |
| **تنظيف `rate_limits`** | دالة `cleanup_rate_limits()` (تُجدوَل لاحقاً). |
| **`normalizeText(kw)` guard** | أضفنا `.length > 0` صراحةً في `detectSubCategory`. |

### الذكاء الاصطناعي (BOQ + المواصفات)
| البند | ماذا عُمل |
|---|---|
| **عدم إزعاج الموردين بالبنود غير المصنّفة** (Critical منتج) | البنود غير المصنّفة **لا تُرسل إطلاقاً**؛ يُرسل المصنّف فقط وتُحجز البقية ليصنّفها المقاول. |
| **Confidence score للتصنيف** | الذكاء يُرجع ثقة (high/medium/low)؛ المنخفضة تُعرض للمراجعة مع إبقاء اقتراح الذكاء. |
| **مطابقة الرموز التقريبية** (DRS-701=DRS701) | `match-spec` يوحّد الرموز (يتجاهل الفواصل/الحالة) ويتسامح مع أخطاء OCR/لغتين. |
| **تكلفة الذكاء** | تصنيف BOQ يستخدم النموذج الأرخص (Haiku) افتراضياً على Anthropic. |

### الثقة والنمو
| البند | ماذا عُمل |
|---|---|
| **Supplier Score 0-100 مرئي للمقاول** | `supplierScore()` من (تحقّق + تقييم + سرعة رد + نسبة فوز + خبرة)؛ شارة ملوّنة على كل عرض في مصفوفة الترسية. |
| **شارات الاعتماد** (أرامكو/سابك/نيوم/روشن…) | عمود `approvals`؛ الأدمن يضبطها من صفحة المستخدم؛ تظهر في مصفوفة الترسية وصفحة العرض. |

### التصميم وتجربة المستخدم
| البند | ماذا عُمل |
|---|---|
| **ألوان القطاعات تتعارض مع البراند** | الكهرباء صار كهرماني `#D97706` بدل البرتقالي (لا يتعارض مع زر الإجراء). |
| **شريط تنقّل سفلي للجوال** | أضيف Bottom Nav (أهم 5 وجهات) + إزاحة زر المساعد حتى لا يتداخل. |
| **خطأ نحوي بالتسمية** («+1 أصناف») | `arItems()`: 1 مادة واحدة، 2 مادتان، 3-10 مواد، 11+ مادة. |
| **بطاقات «ميتة» / حيوية** | وسم «🟢 جديد» للطلبات خلال 12 ساعة + حالة فارغة تشرح وتوجّه. |
| **(منجز سابقاً بنفس الجلسة)** | فروق الإحصائيات، خلاصة النشاط، نبض السوق + الاتجاه، ويدجت اكتمال الحساب، عدّادات المهلة، تسمية احترافية للطلبات. |

---

## ⏸️ أُجِّل / وُضِع جانباً (مع السبب)

### يحتاج تدخّل المالك (لا يُنفّذ بالكود)
| البند | السبب |
|---|---|
| **مفتاح واثق Production** | إعداد حساب واثق + متغيّر بيئة — قرار/وصول المالك. |
| **Admin 2FA / MFA** | إعداد في لوحة Supabase — المالك. |
| **CAPTCHA على التسجيل** | مكوّن Turnstile جاهز، لكن يحتاج مفاتيح الموقع (المالك) + ربط. |
| **Sentry / مراقبة أخطاء** | حساب خارجي — المالك. (يوجد `system_errors_log` كبديل داخلي.) |
| **PITR / النسخ الاحتياطي** | خطة/لوحة Supabase — المالك. |
| **WhatsApp Business API** | حساب Meta/Twilio + توكن — المالك. |

### كبيرة/معمارية (تحتاج جلسة مخصّصة)
| البند | السبب |
|---|---|
| **إزالة `@ts-nocheck` وكتابة الأنواع** | تعديل ضخم عبر عشرات الملفات؛ خطر كسر مرتفع لو تمّ دفعة واحدة — يُنصح تدريجياً من مسارات الـAPI. |
| **نقل Taxonomy لقاعدة البيانات** | إعادة هيكلة كبيرة؛ المحرّك الحالي يعمل — يُؤجَّل لجلسة مخصّصة. |
| **ZATCA Phase-2 (فاتورة)** | تكامل Fatoora + شهادات + اعتماد رسمي — كبير وقانوني؛ يحتاج تسجيل ZATCA. |
| **PWA / Push Notifications** | يحتاج manifest + service worker + بنية push — متوسط/كبير. |
| **Reverse Auction (جولة 2)** | ميزة متوسطة؛ «طلب تخفيض السعر» الحالي يغطّي جزءاً. |
| **Supplier Catalog (منتجات/داتاشيت)** | ميزة كبيرة تحوّل المنصة لـmarketplace — تُؤجَّل. |
| **صفحة أسعار سوق عامة (SEO)** | متوسطة — تُؤجَّل. |
| **Onboarding تدريجي للمورد** | تسجيل المقاول خفيف أصلاً؛ تقصير تسجيل المورد إعادة تصميم أكبر. |

### بيانات/قرار يحتاج المالك
| البند | السبب |
|---|---|
| **فهرس فريد للسجل التجاري** (منع تعدّد الحسابات) | **يوجد سجلات تجارية مكرّرة حالياً** في البيانات؛ لا يمكن فرض القيد قبل تنظيف التكرارات (المالك يقرّر أيّها يبقى). |

### تحسينات مؤجّلة (قيمة أقل/لاحقة)
| البند | السبب |
|---|---|
| **واجهة فرز إداري (Triage Queue)** | غطّينا «عدم الإزعاج» بمنع إرسال غير المصنّف؛ الطابور الإداري الكامل تحسين لاحق. |
| **تعديل مجمّع لتصنيف BOQ (Bulk-edit)** | مفيد للجداول الكبيرة — لم يُنفّذ بعد. |
| **Award Matrix: كشف تدريجي + عمود ثابت بالجوال** | المصفوفة تعمل؛ تحسين عرض لاحق. |
| **اقتباس المواصفات (صفحة/فقرة + اعتماد)** | حالياً تُملأ المواصفة مباشرةً؛ إضافة Citation تحسين. |
| **حلقة تغذية راجعة للـTaxonomy** | `rfq_dismissals.reason` موجود؛ ربطه بتحديث الكلمات تلقائياً لاحق. |
| **فلتر صريح لقناة الرسائل (Realtime)** | الإشعارات مُفلترة per-user ✓؛ قناة الرسائل تشترك عاماً ثم تَعُدّ (RLS يحمي الصفوف) — تحسين فلترة لاحق. |
| **تكبير الخطوط الصغيرة درجة** | لم يُعمَّم (خطر تأثير واسع) — يُنفّذ موضعياً عند الحاجة. |

---

*ملخّص: عولجت كل الـBlockers القابلة للتنفيذ بالكود (الهوية، الأداء، القنص، عدم الإزعاج)
+ ميزات الثقة (السكور + الاعتمادات) + إصلاحات التصميم (الألوان، نقل الجوال، التسمية).
المتبقّي إمّا إعدادات يملكها المالك، أو إعادة هيكلة كبيرة، أو يحتاج تنظيف بيانات.*
